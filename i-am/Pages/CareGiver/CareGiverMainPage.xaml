<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="i_am.Pages.CareGiver.CareGiverMainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:i_am.ViewModels"
    xmlns:local="clr-namespace:i_am.Resources.Strings"
    x:DataType="vm:CareGiverMainViewModel">

    <Shell.TitleView>
        <Label
            FontAttributes="Bold"
            FontSize="30"
            HorizontalOptions="Center"
            Text="I AM"
            TextColor="White"
            VerticalOptions="Center" />
    </Shell.TitleView>

    <Grid Padding="20">
        <VerticalStackLayout
            HorizontalOptions="Fill"
            Spacing="25"
            VerticalOptions="Center">

            <ScrollView>
                <VerticalStackLayout Spacing="10">
                    <Button Command="{Binding GoToNotificationsCommand}" Text="{Binding Source={x:Static local:AppStrings.CareGiver_Notifications}}" />
                    <Button Command="{Binding GoToCalendarCommand}" Text="{Binding Source={x:Static local:AppStrings.CareGiver_Calendar}}" />
                    <Button Command="{Binding GoToStatisticsCommand}" Text="{Binding Source={x:Static local:AppStrings.CareGiver_Statistics}}" />
                    <Button Command="{Binding GoToEditCareTakerQuestionsCommand}" Text="{Binding Source={x:Static local:AppStrings.CareGiver_EditQuestions}}" />
                    <Button Command="{Binding GoToManageCareTakersCommand}" Text="{Binding Source={x:Static local:AppStrings.CareGiver_ManageDependents}}" />
                    <Button Command="{Binding GoToSettingsCommand}" Text="{Binding Source={x:Static local:AppStrings.CareGiver_Settings}}" />
                    <Button Command="{Binding GoToManageAccountCommand}" Text="{Binding Source={x:Static local:AppStrings.CareGiver_ManageAccount}}" />
                </VerticalStackLayout>
            </ScrollView>

            <VerticalStackLayout>
                <Button
                    Margin="0,10,0,0"
                    Command="{Binding LogoutCommand}"
                    Style="{StaticResource DangerButtonStyle}"
                    Text="{Binding Source={x:Static local:AppStrings.CareGiver_Logout}}" />
                <Label
                    Padding="20"
                    HorizontalOptions="Center"
                    Style="{StaticResource SubtitleLabelStyle}"
                    Text="{Binding Source={x:Static local:AppStrings.CareGiver_EmergencyInfo}}">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToInformationCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </Grid>
</ContentPage>