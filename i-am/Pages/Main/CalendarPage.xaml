<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="i_am.Pages.Main.CalendarPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:i_am.ViewModels"
    xmlns:models="clr-namespace:i_am.Models"
    x:DataType="vm:CalendarViewModel"
    Title="Historia Raportów">

    <Grid>
        <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" HorizontalOptions="Center" VerticalOptions="Center" ZIndex="10"/>

        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="20">

                <VerticalStackLayout IsVisible="{Binding IsCareGiver}" Spacing="10">
                    <Label FontAttributes="Bold" Text="Wybierz podopiecznego:" />
                    <Border StrokeShape="RoundRectangle 8" BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}" Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}">
                        <Grid HeightRequest="50">
                            <Label Text="{Binding SelectedCareTakerName}" HorizontalOptions="Start" VerticalOptions="Center" Margin="15,0,0,0" TextColor="{AppThemeBinding Light=Black, Dark=White}" FontSize="15" />
                            <Button Command="{Binding SelectCareTakerCommand}" BackgroundColor="Transparent" HorizontalOptions="Fill" VerticalOptions="Fill" />
                        </Grid>
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout IsVisible="{Binding HasCareTakerSelected}" Spacing="15">

                    <Grid ColumnDefinitions="Auto, *, Auto" VerticalOptions="Center" Margin="0,0,0,10">
                        <Button Grid.Column="0" Text="&lt;" Command="{Binding ChangeMonthCommand}" CommandParameter="Prev" WidthRequest="40" Padding="0" BackgroundColor="{StaticResource Primary}"/>
                        <Label Grid.Column="1" Text="{Binding CurrentMonthName}" FontAttributes="Bold" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center"/>
                        <Button Grid.Column="2" Text="&gt;" Command="{Binding ChangeMonthCommand}" CommandParameter="Next" WidthRequest="40" Padding="0" BackgroundColor="{StaticResource Primary}"/>
                    </Grid>

                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding Days}" Spacing="10">
                        <BindableLayout.EmptyView>
                            <Label Text="Brak raportów w tym miesiącu." HorizontalOptions="Center" TextColor="{StaticResource Gray500}" Margin="0,20,0,0"/>
                        </BindableLayout.EmptyView>

                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="vm:CalendarDayItem">

                                <Border IsVisible="{Binding HasData}" Padding="15" StrokeShape="RoundRectangle 8" BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}" Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CalendarViewModel}}, Path=SelectDayCommand}" CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>

                                    <Grid ColumnDefinitions="*, Auto">
                                        <VerticalStackLayout Grid.Column="0" VerticalOptions="Center" Spacing="2">
                                            <Label Text="{Binding Date, StringFormat='{0:dd MMMM yyyy}'}" FontAttributes="Bold" FontSize="16"/>
                                            <Label Text="{Binding Response.EvaluationStatus}" TextColor="{Binding StatusColor}" FontAttributes="Bold" />
                                        </VerticalStackLayout>
                                        <Button Grid.Column="1" Text="Szczegóły" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CalendarViewModel}}, Path=SelectDayCommand}" CommandParameter="{Binding .}" HeightRequest="35"/>
                                    </Grid>
                                </Border>

                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <BoxView HeightRequest="400" IsVisible="{Binding IsDayDetailsVisible}" Color="Transparent"/>
            </VerticalStackLayout>
        </ScrollView>

        <Border 
            IsVisible="{Binding IsDayDetailsVisible}"
            VerticalOptions="End"
            BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}" 
            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" 
            StrokeShape="RoundRectangle 20,20,0,0">

            <Grid RowDefinitions="Auto, *">
                <Grid Grid.Row="0" ColumnDefinitions="*, Auto" Padding="20">
                    <VerticalStackLayout Grid.Column="0" Spacing="2">
                        <Label Text="{Binding SelectedDay.Date, StringFormat='{0:dd MMMM yyyy}'}" FontAttributes="Bold" FontSize="18"/>
                        <Label Text="{Binding SelectedDay.Response.EvaluationStatus, StringFormat='Status: {0}'}" FontSize="14" TextColor="{StaticResource Primary}"/>
                        <Label Text="{Binding SelectedDay.Response.TotalScore, StringFormat='Suma punktów: {0}'}" FontSize="14" FontAttributes="Bold" IsVisible="{Binding IsCareGiver}"/>
                    </VerticalStackLayout>
                    <Button Grid.Column="1" Text="Zamknij" Command="{Binding CloseDetailsCommand}" BackgroundColor="{StaticResource Gray500}" HeightRequest="35" Padding="10,0" VerticalOptions="Start"/>
                </Grid>

                <ScrollView Grid.Row="1" MaximumHeightRequest="300" IsVisible="{Binding IsCareGiver}">
                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding SelectedDayAnswers}" Padding="20,0,20,20" Spacing="15">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="models:GivenAnswer">
                                <Border Padding="15" StrokeShape="RoundRectangle 8" BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}" Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}">
                                    <VerticalStackLayout Spacing="5">
                                        <Label Text="{Binding QuestionText}" FontAttributes="Bold" FontSize="14"/>
                                        <Label Text="{Binding SelectedOptionText}" TextColor="{StaticResource Primary}" FontAttributes="Bold" />
                                        <Label Text="{Binding OpenTextResponse}" FontAttributes="Italic" TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"/>
                                        <Label Text="{Binding PointsAwarded, StringFormat='Punkty: {0}'}" FontSize="12" TextColor="{StaticResource Gray500}" Margin="0,5,0,0"/>
                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </Border>
    </Grid>
</ContentPage>