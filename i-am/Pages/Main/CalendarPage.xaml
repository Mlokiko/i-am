<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="i_am.Pages.Main.CalendarPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:i_am.ViewModels"
    xmlns:models="clr-namespace:i_am.Models"
    x:DataType="vm:CalendarViewModel"
    Title="Kalendarz">

    <Grid>
        <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" HorizontalOptions="Center" VerticalOptions="Center" ZIndex="10"/>

        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="20">

                <VerticalStackLayout IsVisible="{Binding IsCareGiver}" Spacing="10">
                    <Label FontAttributes="Bold" Text="Wybierz podopiecznego:" />
                    <Border Padding="10,0" StrokeShape="RoundRectangle 8">
                        <Picker ItemsSource="{Binding CareTakers}" ItemDisplayBinding="{Binding Name}" SelectedItem="{Binding SelectedCareTaker}" Title="Wybierz..." />
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout IsVisible="{Binding HasCareTakerSelected}" Spacing="15">

                    <Grid ColumnDefinitions="Auto, *, Auto" VerticalOptions="Center">
                        <Button Grid.Column="0" Text="&lt;" Command="{Binding ChangeMonthCommand}" CommandParameter="Prev" WidthRequest="40" Padding="0"/>
                        <Label Grid.Column="1" Text="{Binding CurrentMonthName}" FontAttributes="Bold" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center"/>
                        <Button Grid.Column="2" Text="&gt;" Command="{Binding ChangeMonthCommand}" CommandParameter="Next" WidthRequest="40" Padding="0"/>
                    </Grid>

                    <Grid ColumnDefinitions="*,*,*,*,*,*,*" Margin="0,10,0,5">
                        <Label Grid.Column="0" Text="Pn" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{StaticResource Gray500}"/>
                        <Label Grid.Column="1" Text="Wt" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{StaticResource Gray500}"/>
                        <Label Grid.Column="2" Text="Śr" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{StaticResource Gray500}"/>
                        <Label Grid.Column="3" Text="Cz" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{StaticResource Gray500}"/>
                        <Label Grid.Column="4" Text="Pt" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{StaticResource Gray500}"/>
                        <Label Grid.Column="5" Text="Sb" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{StaticResource Gray500}"/>
                        <Label Grid.Column="6" Text="Nd" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{StaticResource Gray500}"/>
                    </Grid>

                    <FlexLayout BindableLayout.ItemsSource="{Binding Days}" Wrap="Wrap" Direction="Row" JustifyContent="Start">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="vm:CalendarDayItem">

                                <Border WidthRequest="45" HeightRequest="45" Margin="2" StrokeThickness="2" StrokeShape="RoundRectangle 8">
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CalendarViewModel}}, Path=SelectDayCommand}" CommandParameter="{Binding .}"/>
                                    </Border.GestureRecognizers>

                                    <Border.Triggers>
                                        <DataTrigger TargetType="Border" Binding="{Binding IsEmpty}" Value="True">
                                            <Setter Property="IsVisible" Value="False"/>
                                        </DataTrigger>
                                        <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Stroke" Value="{StaticResource Primary}"/>
                                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource PrimaryLight}, Dark={StaticResource PrimaryDark}}"/>
                                        </DataTrigger>
                                        <DataTrigger TargetType="Border" Binding="{Binding IsSelected}" Value="False">
                                            <Setter Property="Stroke" Value="Transparent"/>
                                            <Setter Property="BackgroundColor" Value="Transparent"/>
                                        </DataTrigger>
                                    </Border.Triggers>

                                    <Grid RowDefinitions="*, Auto">
                                        <Label Grid.Row="0" Text="{Binding DayText}" HorizontalOptions="Center" VerticalOptions="Center" FontAttributes="Bold">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding HasData}" Value="False">
                                                    <Setter Property="TextColor" Value="{StaticResource Gray400}"/>
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <BoxView Grid.Row="1" IsVisible="{Binding HasData}" Color="{Binding StatusColor}" HeightRequest="6" WidthRequest="6" CornerRadius="3" HorizontalOptions="Center" Margin="0,0,0,5"/>
                                    </Grid>
                                </Border>

                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </FlexLayout>
                </VerticalStackLayout>

                <BoxView HeightRequest="400" IsVisible="{Binding IsDayDetailsVisible}" Color="Transparent"/>
            </VerticalStackLayout>
        </ScrollView>

        <Border 
            IsVisible="{Binding IsDayDetailsVisible}"
            VerticalOptions="End"
            BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}" 
            Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" 
            StrokeShape="RoundRectangle 20,20,0,0"
            Shadow="{Shadow Brush=Black, Offset='0,-5', Radius=15, Opacity=0.1}">

            <Grid RowDefinitions="Auto, *">
                <Grid Grid.Row="0" ColumnDefinitions="*, Auto" Padding="20">
                    <VerticalStackLayout Grid.Column="0">
                        <Label Text="{Binding SelectedDay.Date, StringFormat='{0:dd MMMM yyyy}'}" FontAttributes="Bold" FontSize="18"/>
                        <Label Text="{Binding SelectedDay.Response.EvaluationStatus, StringFormat='Status: {0}'}" FontSize="14" TextColor="{StaticResource Primary}"/>
                        <Label Text="{Binding SelectedDay.Response.TotalScore, StringFormat='Suma punktów: {0}'}" FontSize="14" FontAttributes="Bold" IsVisible="{Binding IsCareGiver}"/>
                    </VerticalStackLayout>
                    <Button Grid.Column="1" Text="Zamknij" Command="{Binding CloseDetailsCommand}" BackgroundColor="{StaticResource Gray500}" HeightRequest="35" Padding="10,0"/>
                </Grid>

                <ScrollView Grid.Row="1" MaximumHeightRequest="300" IsVisible="{Binding IsCareGiver}">
                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding SelectedDayAnswers}" Padding="20,0,20,20" Spacing="15">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="models:GivenAnswer">
                                <Border Padding="10" StrokeShape="RoundRectangle 8">
                                    <VerticalStackLayout Spacing="5">
                                        <Label Text="{Binding QuestionText}" FontAttributes="Bold" FontSize="14"/>
                                        <Label Text="{Binding SelectedOptionText}" TextColor="{StaticResource Primary}" />
                                        <Label Text="{Binding OpenTextResponse}" FontAttributes="Italic" />
                                        <Label Text="{Binding PointsAwarded, StringFormat='Punkty: {0}'}" FontSize="12" TextColor="{StaticResource Gray500}"/>
                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </Border>
    </Grid>
</ContentPage>