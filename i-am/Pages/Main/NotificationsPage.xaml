<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="i_am.Pages.Main.NotificationsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:i_am.Models"
    xmlns:vm="clr-namespace:i_am.ViewModels"
    x:Name="NotificationsRoot"
    Title="Powiadomienia"
    x:DataType="vm:NotificationsViewModel">

    <Grid Padding="20">

        <CollectionView ItemsSource="{Binding NotificationsList}">

            <CollectionView.EmptyView>
                <VerticalStackLayout
                    HorizontalOptions="Center"
                    Spacing="10"
                    VerticalOptions="Center">
                    <Label
                        FontSize="50"
                        HorizontalOptions="Center"
                        Text="📭" />
                    <Label
                        FontSize="18"
                        HorizontalOptions="Center"
                        Text="Brak nowych powiadomień"
                        TextColor="{AppThemeBinding Light={StaticResource Gray500},
                                                    Dark={StaticResource Gray400}}" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:AppNotification">

                    <Border
                        Margin="0,0,0,15"
                        Padding="15"
                        BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight},
                                                          Dark={StaticResource SurfaceDark}}"
                        Stroke="{AppThemeBinding Light={StaticResource Gray200},
                                                 Dark={StaticResource Gray600}}"
                        StrokeShape="RoundRectangle 12">

                        <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto">

                            <VerticalStackLayout
                                Grid.Row="0"
                                Grid.Column="0"
                                Spacing="5">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="16"
                                    Text="{Binding Title}" />
                                <Label
                                    FontSize="14"
                                    Text="{Binding Message}"
                                    TextColor="{AppThemeBinding Light={StaticResource Gray500},
                                                                Dark={StaticResource Gray400}}" />

                                <Label
                                    FontSize="12"
                                    Text="{Binding CreatedAt, StringFormat='{0:dd.MM.yyyy HH:mm}'}"
                                    TextColor="{AppThemeBinding Light={StaticResource Gray400},
                                                                Dark={StaticResource Gray500}}" />
                            </VerticalStackLayout>

                            <Label
                                Grid.Row="0"
                                Grid.Column="1"
                                FontAttributes="Bold"
                                FontSize="20"
                                HorizontalOptions="End"
                                Text="✕"
                                TextColor="{AppThemeBinding Light={StaticResource Gray500},
                                                            Dark={StaticResource Gray400}}"
                                VerticalOptions="Start">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding BindingContext.DismissNotificationCommand, Source={x:Reference NotificationsRoot}}" CommandParameter="{Binding .}" />
                                </Label.GestureRecognizers>
                            </Label>

                            <Button
                                Grid.Row="1"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                Margin="0,15,0,0"
                                BackgroundColor="{AppThemeBinding Light={StaticResource Gray500},
                                                                  Dark={StaticResource Gray600}}"
                                Command="{Binding BindingContext.NotificationActionCommand, Source={x:Reference NotificationsRoot}}"
                                CommandParameter="{Binding .}"
                                CornerRadius="8"
                                HeightRequest="40"
                                HorizontalOptions="End"
                                Text="Zrozumiałem"
                                TextColor="White">
                                <Button.Triggers>
                                    <DataTrigger
                                        Binding="{Binding Type}"
                                        TargetType="Button"
                                        Value="NewInvitation">
                                        <Setter Property="Text" Value="Przejdź do zaproszeń" />
                                        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>

                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </Grid>
</ContentPage>
