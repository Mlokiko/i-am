<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="i_am.MainPage">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="15">

            <Label Text="Firestore Demo" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" />

            <Entry x:Name="NameEntry" 
                   Placeholder="Enter Name" />

            <Entry x:Name="AgeEntry" 
                   Placeholder="Enter Age" 
                   Keyboard="Numeric" />

            <Button Text="Add User to Firestore" 
                    Clicked="OnAddUserClicked" 
                    BackgroundColor="Green" />

            <Button Text="Load Users from Firestore" 
                    Clicked="OnLoadUsersClicked" 
                    BackgroundColor="Blue" />

            <BoxView HeightRequest="1" Color="Gray" Margin="0,10" />

            <Label Text="Database Records:" FontAttributes="Bold" />

            <CollectionView x:Name="UsersCollectionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,5" Padding="10" BorderColor="LightGray">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Age, StringFormat='Age: {0}'}" TextColor="Gray" />
                                <Label Text="{Binding Id, StringFormat='ID: {0}'}" FontSize="10" TextColor="LightGray" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
